merger:
  blockerLabel: /hold
  maxGoroutines: 10
  targetUrl: http://targeturl
  squashLabel: squash
  policy:
    skipUnknownContexts: false
    requiredContexts:
      replace: false
      items:
        - context3
    requiredIfPresentContexts:
      replace: true
      items:
        - context5
        - context6