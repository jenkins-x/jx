merger:
  blockerLabel: /hold
  maxGoroutines: 10
  targetUrl: http://targeturl
  squashLabel: squash
  mergeMethod: rebase
  policy:
    skipUnknownContexts: false
    requiredContexts:
      replace: false
      entries:
        - context3
    requiredIfPresentContexts:
      replace: true
      entries:
        - context5
        - context6